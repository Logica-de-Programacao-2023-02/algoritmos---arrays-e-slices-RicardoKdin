package main

import "fmt"

func main() {

	var x = [7]int{1, 2, 3, 4, 5, 6, 7}
	var valor int

	fmt.Print(`Informe um valor: `)
	fmt.Scan(&valor)

	x[0] += valor
	x[6] += valor

	fmt.Println(`Resultado: `, x)

}
