package main

import "fmt"

func main() {

	var slice = []int{1, 2, 3, 4, 5, 6, 7, 8}
	var x int
	var y int

	fmt.Print(`Informe dois índices: `)
	fmt.Scan(&x, &y)

	slice[x], slice[y] = slice[y], slice[x]

	fmt.Println(slice)

}
